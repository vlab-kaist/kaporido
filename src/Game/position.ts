export const mapSize = 5.0;
export const mapPlaceCount = 9;

export const containerWidth = mapSize + 0.2;
export const containerHeight = mapSize / mapPlaceCount * (mapPlaceCount + 1.5) + 0.2;

export const placePadding = mapSize / mapPlaceCount;
export const placeSize = (mapSize - 0.8) / mapPlaceCount;
export const placeMargin = 0.8 / mapPlaceCount

export const blockerCount = 10;
